#!/usr/bin/env python3
"""
MCP Search Bridge — free search + extract for AI agents.
Zero API keys. Replaces Firecrawl, Exa, Brave Search with free alternatives.

Copy this template, install optional deps (trafilatura, httpx, scrapling),
and configure your agent platform (see references/agent-configs.md).
"""
# Full source: ~/.local/bin/mcp-bridge on the build machine.
# This template is a stub — copy the canonical version from:
#   /home/sethengine/.local/bin/mcp-bridge
# Or regenerate from the skill's SKILL.md architecture description.
print("This is a template stub. Copy the canonical bridge from the build machine.", file=__import__("sys").stderr)
